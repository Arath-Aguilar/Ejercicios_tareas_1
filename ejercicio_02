#include <stdio.h>
#include <stdlib.h>

int main()
{
int a,b;
printf("introduce el primer valor: ");
scanf("%d",&a);
printf("introduce el segundo valor: ");
scanf("%d",&b);
if(a % b==0){
    printf("es divisible");
}
else{
        printf("no es divisible");
}
}
